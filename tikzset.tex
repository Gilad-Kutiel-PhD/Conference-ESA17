\usetikzlibrary{
	positioning
}

\tikzset{label/.style={
	midway,
	sloped,
	draw=none,
	fill=white,
	inner sep=0,
	minimum height=.8em,
	minimum width=.8em,
}}
\tikzset{label below/.style={label, below}}
\tikzset{label above/.style={label, above}}
\tikzset{default node/.style={
	draw, 
	circle,
	inner sep=0mm,
	minimum size=5mm,
	very thick,
	font=\small,
	black!70,
}}

\tikzset{
	>=latex,
	->,
	every node/.style={default node},
}
